<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.helloworld.dao.FirstClassDao">	
	
	<select id="checkUser" resultType="Integer" >
		select count(1) from account where username = #{username} and password = #{password} and status = 'TRUE'
	</select>
	
	<insert id="addUser" parameterType="com.helloworld.bean.Account" >
		<![CDATA[	
			insert into account(username,password,status) values(#{username},#{password},'FALSE')
		]]>
	</insert>
	
	<update id="updateStatusById" parameterType="Long">
		update account set status = 'TRUE' where id = #{id}
	</update>
	
	<update id="updateAccountById" parameterType="com.helloworld.bean.Account">
		update account set username = #{username},password = #{password} where id = #{id}
	</update>
	
</mapper>